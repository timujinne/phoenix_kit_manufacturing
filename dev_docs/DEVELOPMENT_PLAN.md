# PhoenixKit Manufacturing — план развития (черновик для обсуждения)

Дата: 2026-07-09. Статус: модуль-заглушка подключён (dashboard в админ-меню под Складом). Ниже — предлагаемая архитектура и этапы, всё открыто для обсуждения.

## 0. Что уже есть (техническая база)

- Форк `timujinne/phoenix_kit_manufacturing` (upstream `BeamLabEU/phoenix_kit_manufacturing`), подключён в andi как path dep — та же схема, что у склада.
- Регистрация через `use PhoenixKit.Module`: ключ `manufacturing`, включение через Settings (`manufacturing_enabled`), пункт меню `priority: 154` в группе `:admin_main` — сразу под Складом (153).
- Роуты генерируются автоматически из `admin_tabs/0` (`live_view:` в Tab) — router.ex в andi не трогаем.
- Пока без таблиц БД и миграций.

## 1. Станки (Machines) — справочник

Первый реальный этап: простой CRUD-справочник по образцу складских справочников.

Предлагаемый минимум полей (поля не финализированы — на обсуждение):
- `name`, `code` (короткий код/инвентарный номер), `status` (active / maintenance / decommissioned)
- `machine_type` — тип/группа (фрезер, станок с ЧПУ, пресс…) — отдельный мини-справочник или enum?
- `location` — привязка к складу/помещению (есть `phoenix_kit_locations`)
- `description`, `metadata` (JSONB — под неясные пока поля, чтобы не плодить миграции)
- soft delete + activity log (как в складе)

Открытые вопросы:
- Нужны ли паспортные данные (мощность, рабочая зона, дата ввода в эксплуатацию, график ТО)?
- Станок = ресурс с календарём загрузки (для планирования) или пока просто карточка?

## 2. Документ производства (Production Order)

По аналогии с документами склада (internal_orders / goods_issues / goods_receipts):

- Шапка: номер, дата, статус (draft → in_progress → done → cancelled), станок(и), ответственный, срок.
- Строки: **вход** (материалы/заготовки — позиции каталога, количество) и **выход** (готовая продукция, количество). Фактически упрощённая спецификация (BOM).
- `source_refs` JSONB — та же развязка, что в складе: документ производства может ссылаться на заказ / подзаказ / проект, не зная их таблиц.
- Позже: операции/этапы внутри документа (маршрутная карта), время по операциям.

Вопрос: нужен ли отдельный справочник «Спецификации (BOM)» (шаблон: из чего делается изделие), из которого документ производства заполняется одной кнопкой? Предлагаю в v2, не сразу.

## 3. Связь со складом (передача в производство)

Склад уже спроектирован под это — контракт `source_kinds` (`PhoenixKitWarehouse.SourceKinds`): хост регистрирует в конфиге виды источников с колбэками search/resolve/build_lines, и складские документы (Internal Orders, Goods Issues) умеют ссылаться на них и строить строки.

Схема потока:
1. **Материалы в производство**: производственный документ регистрируется как source kind `production_order` → со склада создаётся Goods Issue (расход) «в производство», строки строятся из входных строк производственного документа (`build_lines`).
2. **Готовая продукция на склад**: по завершении производства — Goods Receipt (приход) с ссылкой на тот же документ.
3. Остатки/резервы считает склад (stock_ledger / committed_quantities) — производство своих остатков не ведёт, только ссылается.

Это ничего не требует менять в складском модуле — регистрация делается в конфиге andi (как уже сделано для order/sub_order через `Andi.Warehouse.Integration`).

## 4. Интеграции с другими модулями

- **Dashboards**: контракт `phoenix_kit_widgets/0` (плоские мапы, без зависимости от phoenix_kit_dashboards). Виджеты: «производственные заказы по статусам», «загрузка станков», «просроченные». Образец — HelloWidget в hello_world.
- **Сотрудники (phoenix_kit_staff)**: ответственный за документ, операторы станков/операций (employments/departments уже есть). Ссылки — через uuid + resolve, без жёсткой зависимости.
- **Проекты (phoenix_kit_projects)**: производственный документ ← проект (source_refs), у проектов уже есть Gantt/календарь — потенциально показывать производственные заказы на таймлайне проекта. Механику связи предлагаю обсудить после появления справочника станков.
- **Каталог (phoenix_kit_catalogue)**: строки документа производства = позиции каталога (как в складе).
- **Права**: `permission_metadata/0` уже заявлен — при появлении разделов добавить отдельные permissions (view/manage), как в складе.

## 5. Предлагаемые этапы

1. **v0.1 (сделано)** — заглушка dashboard + меню + инфраструктура модуля.
2. **v0.2 — Станки**: таблица + CRUD + activity log; поля минимальные, `metadata` JSONB под остальное. Появляется первая миграция (см. вопрос про миграции ниже).
3. **v0.3 — Документ производства**: шапка + строки вход/выход, статусы, ссылки source_refs.
4. **v0.4 — Связка со складом**: source kind `production_order`, кнопки «выдать материалы» / «оприходовать продукцию».
5. **v0.5 — Виджеты dashboards + сотрудники/проекты**.
6. Дальше: BOM-шаблоны, маршрутные карты/операции, планирование загрузки станков.

## 6. Технические вопросы на обсуждение

1. **Миграции**: у склада таблицы живут в versioned-миграциях ядра phoenix_kit, своих миграций в репо модуля нет. Для manufacturing так же (таблицы добавляются в ядро, PR в phoenix_kit) или свой `migration_module/0` внутри модуля? От этого зависит процесс релизов.
2. **Поля станков** — см. §1 и §А.2.
3. **Нумерация документов** — своя серия (MFG-0001) или общий механизм нумерации складских документов?
4. **Меню**: пока один пункт Dashboard; при появлении станков/документов — подпункты (parent: :manufacturing), как у склада.

---

## А. Дополнение 2026-07-10: анализ Цеховик ERP и расширенная дорожная карта

Разобран функционал cehovik-erp.ru (production-ERP для мебельных цехов): скриншоты интерфейса + база знаний (справочники Цехи/Участки/Операции/Склады, модуль «Производство» — 18 статей). Связанные планы: `phoenix_kit_locations/dev_docs/DEVELOPMENT_PLAN.md`, `phoenix_kit_warehouse/dev_docs/DEVELOPMENT_PLAN.md`.

### А.1. Модель Цеховика (эталон для сверки)

- **Цех** — группировка участков (имя, цвет, комментарий). Примеры: производственный, токарный/фрезеровочный, малярный, сборочный, упаковочный.
- **Участок** — рабочий пост/станок, где операторы выполняют операции: родительский цех, вид операции, оборудование (опц.), норма выработки (опц.), настройки канбана (строгая очередь этапов, «только готовые к выполнению», бригадный режим), месячное расписание работы (мощность в часах).
- **Операция** (справочник) — норма времени + расценка + ед. изм.; статусы старта/завершения (двигают статус заказа); операция обязана быть привязана к участку; при добавлении в изделие цена/время подтягиваются и переопределяются, есть массовое обновление.
- **Техкарта** — вкладки: Состав, Операции, Итоговые операции, Материалы, Характеристики, Цены, Связанные документы, Файлы, История изменений. Порождается из производственного заказа (ПЗ), генерирует этапы производства.
- **Оперативное планирование** — Гант по участкам (час/день/неделя/…), статусы этапов (В плане / В работе / На паузе / Завершён), приоритеты заказов 1–100 с автоперепланированием, пересчёт плановых дат, буфер 1 день между операциями, красная подсветка при конфликте срока и прогноза.
- **Панель производства** — карточка каждого участка live: вид операции, статус (работает/остановлен), выполнено деталей, текущий заказ/деталь, заработано, чистое время/простой/паузы, брак (%), производительность, прогноз завершения; вкладки Статистика/Операторы/Техкарты.
- **Расчёт даты выпуска** — по нормировкам + расписанию участков; **СГПП** — очередь изготовленной продукции на приёмку складом ГП.
- **Мобильное приложение оператора** — очередь этапов по участку, взять в работу/завершить, файлы, сдельный заработок.
- **Сдельная ЗП** — отчёт по операторам (кол-во операций, сумма, начислено/заработано, XLS/PDF).
- **Брак** — фиксация и отслеживание по участкам/операциям.

### А.2. Проекция на нашу архитектуру

- **Цех** = `Location` типа Workshop (phoenix_kit_locations), **зона/участок-место** = `Space` (kind zone/section). Производственная семантика участка (вид операции, нормы, расписание, канбан-настройки) — в manufacturing: сущность **WorkCenter (Участок)** со ссылками `location_uuid` + `space_uuid`. Место отвечает «где», WorkCenter — «как работает».
- **Станок (Machine)**: паспорт (производитель, модель, серийный №, инв. №, год, мощность, габариты рабочей зоны, дата ввода, график ТО/гарантия до), `machine_type` (справочник), статус (active/maintenance/decommissioned), местоположение (`location_uuid`+`space_uuid`), **файлы** (инструкции, гарантии — мульти-ресурсный Attachments-паттерн, как в locations: folder_uuid/featured_image_uuid в `data` JSONB), произвольные поля в `metadata`, activity log. Станок закрепляется за WorkCenter (0..1) — участок может быть и ручным (без станка), и агрегировать станок + операторов.
- **Виды операций / Операции**: справочник операций с нормой времени, расценкой, ед. изм.; операция привязана к WorkCenter (или к виду операции, который умеют выполнять несколько участков — решить при проектировании планирования).
- **Этапы производства**: строки-операции производственного заказа, распределяемые по участкам — фундамент для канбана и Ганта.

### А.3. Расширенная дорожная карта (уточняет §5)

1. **v0.2 — Станки**: справочник Machine + machine_types + файлы (Attachments) + привязка к locations (нужен locations v0.3: kinds zone/section; пикер места — locations v0.5).
2. **v0.25 — Участки и операции**: WorkCenter (цех/зона/станок/операторы) + справочник операций (норма, расценка, ед. изм.).
3. **v0.3 — Производственный заказ**: шапка + строки вход/выход + этапы-операции по участкам, статусы, source_refs.
4. **v0.4 — Склад**: source kind `production_order`, выдача материалов / приёмка ГП, резервы; очередь «готово к приёмке» (аналог СГПП).
5. **v0.5 — Виджеты + сотрудники/проекты** (как в §5.5).
6. **v0.6 — Техкарты (BOM-шаблоны)**: материалы + операции изделия, заполнение производственного заказа одной кнопкой; массовое обновление цен/норм.
7. **v0.7 — Панель производства + канбан**: live-карточки участков (PubSub), очередь этапов, фиксация брака.
8. **v0.8 — Планирование**: расписание участков, приоритеты, расчёт плановых дат/даты выпуска; Гант (у phoenix_kit_projects уже есть Gantt — оценить переиспользование).
9. **v0.9 — Рабочее место оператора (mobile-first LiveView)**: очередь этапов участка, взять/завершить/пауза, файлы этапа; сдельная выработка → отчёт по операторам (phoenix_kit_staff).

Принцип этапности тот же: каждый шаг — самостоятельно полезный справочник/документ; планирование и ЗП — только после того, как накопится фактура (нормировки, этапы, операторы).

## Б. Решения, принятые 2026-07-10 (обсуждение с владельцем)

Фундамент = справочник станков; склад из производства не трогаем (связка позже через source_kinds); ЗП/финансы и мобильный/планшетный интерфейс — за скобками.

1. **Паспорт станка**: ядро колонками — название, код/инв. №, тип, статус (active / maintenance / repair / mothballed / decommissioned), место (`location_uuid` + `space_uuid`, мягкие ссылки на phoenix_kit_locations), производитель, модель, серийный №, год выпуска, дата ввода, гарантия до, даты ТО (последнее/интервал/следующее), заметки. Остальные характеристики — `metadata` JSONB по **шаблону типа станка**: `field_template` = JSON-список `{key, label, type: text|number|date|boolean|select, unit, required}` (концепция из phoenix_kit_entities, реализация локальная, без зависимости).
2. **Тип станка** — отдельный справочник (name, description, field_template, status).
3. **Операции** — глобальный справочник (название, ед. изм., базовая норма времени; расценок нет — ЗП за скобками). Связка станок↔операция — таблица с опциональным полем-переопределением нормы (пусто = базовая норма операции). Эта норма — будущий вход планирования.
4. **Брак**: справочник «Причины брака» — сразу; записи брака (кол-во, причина, этап/операция/станок) — вместе с этапами производственного заказа (v0.3). Акт брака документом — позже при потребности.
5. **ТО**: только поля-даты в паспорте + статус «на ТО»; журнал обслуживания — отдельной сущностью позже (пока история — комментариями).
6. **Файлы**: паттерн locations-Attachments (папка + заглавное фото в `data` JSONB, мульти-ресурсный), НЕ складской storage_folder-колоночный. Фото станка в карточке/списке.
7. **Обвязка**: копируем из warehouse как паттерн (не как зависимость): ColumnConfig-движок, ColumnManagement, ViewConfigs (хранение в phoenix_kit_settings, ключ `manufacturing_view_config:*`), ActivityLog-обёртку, Comments-обёртку (resource_type `machine`). Индекс без streams (справочная кардинальность), форма — вкладки-роуты (hidden_crud_tabs, как у internal_orders).
8. **Миграции — решено**: собственный `migration_module/0` в модуле (механизм ядра подтверждён: автообнаружение через ModuleDiscovery, `mix phoenix_kit.update` генерирует обёртку в host `priv/repo/migrations`). Мы — первый потребитель: перед реализацией сверить сигнатуры (`migrated_version_runtime/1`, `current_version/0`) с исходником `PhoenixKit.Migrations.Postgres`; идемпотентные IF NOT EXISTS-гарды + `@disable_ddl_transaction` (PgBouncer). Таблицы: `phoenix_kit_manufacturing_*`. **[SUPERSEDED 2026-07-12]**: миграции консолидированы в core V143, `migration_module/0` удалён — см. PR `core-v143-module-tables`.
9. **Проекты/заказы**: мягкие ссылки source_refs; этапы производства отдаются в таймлайн проекта через провайдер-контракт — Gantt живёт только в phoenix_kit_projects, производство таймлайн не дублирует.
10. **Меню**: подпункты Machines / Machine types / Operations (parent: :manufacturing, priorities 155–157) + hidden CRUD tabs; рестарт для discovery.


## В. Gap-анализ после upstream v0.2.0 (2026-07-11)

Мейнтейнер реализовал Machines v0.2 сам. Сверка с §Б (полный отчёт — исследование 2026-07-11):

| §Б | Статус upstream | Что осталось нам (волна v0.2.x) |
|---|---|---|
| 1. Паспорт станка | PARTIAL: name/code/manufacturer/serial/description/location_note(текст)/status(3)/data/metadata | model, год, ввод в экспл., гарантия, даты ТО; статусы +repair/+mothballed; location_uuid+space_uuid (наш PlacePicker из locations v0.5!) вместо/рядом с location_note; field_template на типе + динамический рендер metadata |
| 2. Типы станков | SHIPPED (M2M assignments) | только field_template (см. выше) |
| 3. Операции | MISSING | справочник (name, unit, норма) + M2M станок↔операция с переопределением нормы |
| 4. Причины брака | MISSING | справочник |
| 5. Даты ТО | MISSING | поля в паспорт (журнал — отложен, как решено) |
| 6. Файлы/фото | MISSING | locations-Attachments паттерн (folder_uuid/featured_image_uuid в data), фото в карточке/списке |
| 7. Обвязка | PARTIAL: activity log есть; index без streams есть; hidden CRUD tabs есть | ColumnConfig/ColumnManagement/ViewConfigs (warehouse-паттерн) + Comments (resource_type "machine") |
| 8. Миграции | SHIPPED (migration_module, V1, идемпотентность) | добавить @disable_ddl_transaction; новые поля/таблицы = V2+ |
| 9. Проекты/заказы | ожидаемо нет (v0.3+) | вне волны |
| 10. Меню | SHIPPED (Dashboard/Machines/Types 155-157) | + подтаб Operations |

Принцип волны: **структура и стиль мейнтейнера — базис** (single-page card формы, его схемы/миграции расширяем V2, его конвенции UI), наши дополнения — поверх, PR в upstream по готовности.

## Г. Entities migration (волна E, 2026-07-12)

Дополнение к §В — статус строк 2-4 таблицы гэп-анализа изменился. «Типы
станков», «Операции» и «Причины брака» реализованы (§Б.2-4 выполнены), но не
как модуль-собственные схемы/контексты, а как справочники на базе
`phoenix_kit_entities` (entity blueprints `machine_type`/`operation`/
`defect_reason`, миграция V5) — по прецеденту андийских
`Andi.Orders.StatusRegistry`/`SuborderTypes`. Полное обоснование и разбивка
задач — `dev_docs/ENTITIES_MIGRATION_SPEC.md` и
`dev_docs/IMPLEMENTATION_PLAN_E.md`. На практике:

- Чтение/пикеры идут через `PhoenixKitManufacturing.EntitiesRegistry` (ETS
  + PubSub-инвалидация), а не через модульные контексты `Operations`/
  `DefectReasons` или CRUD-функции вида `Machines.create_machine_type/2` —
  все они удалены волной E.
- CRUD — generic entities admin UI (`/admin/entities/machine_type/data`,
  `/admin/entities/operation/data`, `/admin/entities/defect_reason/data`),
  не собственные LiveView-формы модуля (тоже удалены).
- `field_template` (§Б.1/строка 1 выше) пережил перенос как
  `metadata["field_template"]` на записи entity `machine_type` — generic
  entities-форма не даёт UI для недекларируемых полей; мини-редактор
  реализован задачей `E18` (`Web.MachineTypeTemplateLive`, скрытый роут
  `manufacturing/machine-types/:uuid/template`), вне generic entities CRUD.
